/** @format */

import {AppRegistry} from 'react-native';
import UnsplashApp from './app/index';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => UnsplashApp);
